/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "www.soorban.com",
      "avatars.githubusercontent.com",
    ],
  },
};

module.exports = nextConfig;
